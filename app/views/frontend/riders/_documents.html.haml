- if @rider.id_proof_documents.present?
  -@rider.id_proof_documents.each do |image|
    .col-xs-6.col-md-2.col-sm-2.col-lg-3
      .show_document_thumbnail{:id => image["public_id"]}
        = image_tag(image["thumbnail_url"])
        /PUKTIJAM for copy profile
        - if @rider.id?
          = link_to "Delete", image_path(:id => @rider.id, :resource_type => @rider.class, :image_type => image["image_type"], :img_pub_id => image["public_id"] ), :method => :delete, data: { confirm: "Are you sure you want to delete this image?" , :remote => true}
  
  /Temporarily suspended delete all func, beacuse its not implemented yet, does't seemd urgently required 
  /.row
  /.deleteAllDocs
  /  = link_to "Delete All", "#", :method => :get, data: { confirm: "Are you sure you want to delete ALL images?" }, :class => 'btn btn-default'      
/ :javascript
/   $(document).ready(function() {
/       $('.deleteAllDocs').click(function () {
/           var Url = "#{Rails.application.routes.url_helpers.remove_all_rider_images_path(@rider)}"
/           $('#images').empty();
/           $.get(Url);
/           return false;
/       });
/   });

/  